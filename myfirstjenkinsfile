/* Requires the Docker Pipeline plugin */
pipeline {
    agent any
    stages {
        stage('Say hello') {
            steps {
                sh 'echo "hello from Jenkins" >> helloJenkins'
            }
        }
    }
}
