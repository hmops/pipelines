/* Requires the Docker Pipeline plugin */
pipeline {
    agent any
    stages {
        stage('Say hello') {
            steps {
                sh 'echo "hello from Jenkins 2" >> helloJenkins'
                sh '''
                    echo "run multiple commands"
                    ls -lah
                '''
            }
        }
    }
}
