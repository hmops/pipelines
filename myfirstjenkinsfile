/* Requires the Docker Pipeline plugin */
pipeline {
    agent any
    stages {
        stage('Say hello') {
            steps {
                echo "hello from Jenkins" >> helloJenkins
            }
        }
    }
}
